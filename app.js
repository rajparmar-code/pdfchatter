const $=s=>document.querySelector(s);const modal=$('#modal');let signup=false;
function openAuth(mode=false){signup=mode;$('#authKicker').textContent=signup?'GET STARTED':'WELCOME BACK';$('#authTitle').textContent=signup?'Create your PDFChatter account':'Sign in to PDFChatter';$('#authSub').textContent=signup?'A calmer way to understand what you read.':'Pick up your thinking where you left off.';$('#authSubmit').innerHTML=signup?'Create account <span>↗</span>':'Sign in <span>↗</span>';$('#switchText').textContent=signup?'Already have an account?':'New here?';$('#switchAuth').textContent=signup?'Sign in instead':'Create an account';modal.classList.add('open');setTimeout(()=>$('.auth-card input').focus(),80)}
$('#loginBtn').onclick=()=>openAuth(false);$('#signupBtn').onclick=()=>openAuth(true);$('#heroStart').onclick=()=>openAuth(true);$('#closeModal').onclick=()=>modal.classList.remove('open');modal.onclick=e=>{if(e.target===modal)modal.classList.remove('open')};$('#switchAuth').onclick=()=>openAuth(!signup);document.addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.remove('open')});
$('#themeBtn').onclick=()=>{document.body.classList.toggle('dark');localStorage.setItem('pdfchatter-theme',document.body.classList.contains('dark')?'dark':'light')};if(localStorage.getItem('pdfchatter-theme')==='dark')document.body.classList.add('dark');
$('#authForm').onsubmit=e=>{e.preventDefault();const b=$('#authSubmit');b.innerHTML='Connecting…';setTimeout(()=>{b.innerHTML=signup?'Account created ✓':'Signed in ✓';b.style.background='#739b30';setTimeout(()=>modal.classList.remove('open'),600)},700)};
$('#uploadBtn').onclick=()=>alert('PDF upload will connect after Supabase and Groq keys are added.');$('#demoBtn').onclick=()=>document.querySelector('#workspace').scrollIntoView({behavior:'smooth'});document.querySelectorAll('.suggestions button').forEach(b=>b.onclick=()=>alert('Upload a PDF first to ask questions about it.'));
